# shura-citrus-classifier
Computer vision system for citrus export quality classification. OpenCV + HSV segmentation + morphological analysis → 3-class grading (Export/Local/Reject). CLI + Tkinter GUI, CSV/JSON export.
